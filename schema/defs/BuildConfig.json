{
  "type": "object",
  "additionalProperties": false,
  "description": "Instructions for building a custom container image",
  "properties": {
    "base": {
      "type": "string",
      "description": "Base image to build from (default: \"coi\")"
    },
    "script": {
      "type": "string",
      "description": "Path to a build shell script (relative to profile directory or absolute)"
    },
    "commands": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Inline build commands as an alternative to a script file"
    },
    "compression": {
      "type": "string",
      "description": "Image compression algorithm (e.g. \"none\", \"gzip\", \"xz\"; empty = Incus default)"
    },
    "agents": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "AI agents to install in the base image (e.g. [\"claude\", \"opencode\"]); empty/unset installs all supported agents"
    }
  }
}

{
  "type": "object",
  "additionalProperties": false,
  "description": "Security monitoring and threat response settings",
  "properties": {
    "enabled": {
      "type": "boolean",
      "description": "Enable background security monitoring"
    },
    "auto_pause_on_high": {
      "type": "boolean",
      "description": "Automatically pause the container when a high-severity threat is detected"
    },
    "auto_kill_on_critical": {
      "type": "boolean",
      "description": "Automatically kill the container when a critical-severity threat is detected"
    },
    "poll_interval_sec": {
      "type": "integer",
      "minimum": 0,
      "description": "How often to collect container stats (seconds)"
    },
    "file_read_threshold_mb": {
      "type": "number",
      "minimum": 0,
      "description": "Alert when the container reads more than this many MB in a single poll interval"
    },
    "file_read_rate_mb_per_sec": {
      "type": "number",
      "minimum": 0,
      "description": "Alert when the sustained file read rate exceeds this threshold (MB/s)"
    },
    "audit_log_retention_days": {
      "type": "integer",
      "minimum": 0,
      "description": "Number of days to retain audit log files"
    },
    "process_count_threshold": {
      "type": "integer",
      "minimum": 0,
      "description": "Maximum process count before a fork-bomb alert (0 = disabled)"
    },
    "process_spawn_rate_threshold": {
      "type": "integer",
      "minimum": 0,
      "description": "Maximum processes spawned per poll interval before an alert (0 = disabled)"
    },
    "nft": {
      "$ref": "#/$defs/NFTMonitoringConfig"
    },
    "forensics_on_kill": {
      "type": "boolean",
      "description": "Preserve a forensic COPY of the container (incus copy, capped at 3 per container, oldest pruned) before an auto-kill on a critical threat deletes it — the copy is made while the container still runs and survives the kill (the original still auto-deletes as usual); the kill fires exactly when the container's state is most worth investigating. Best-effort: a failed copy never blocks or delays the response. Opt-in; default: false."
    }
  }
}

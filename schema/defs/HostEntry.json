{
  "type": "object",
  "additionalProperties": false,
  "description": "One IPv4 address mapped to one or more hostnames for the container's /etc/hosts",
  "required": ["ip", "hostnames"],
  "properties": {
    "ip": {
      "type": "string",
      "description": "IPv4 address the hostnames resolve to (e.g. \"10.0.0.5\")"
    },
    "hostnames": {
      "type": "array",
      "items": { "type": "string" },
      "minItems": 1,
      "description": "One or more hostnames that resolve to this address inside the container"
    },
    "ports": {
      "type": "array",
      "items": { "type": "integer", "minimum": 1, "maximum": 65535 },
      "description": "Optional TCP/UDP destination ports this host is reachable on (e.g. [443]). Scopes the firewall reachability for this host only, so in restricted mode you can open a single LAN service on one port while the rest of egress stays open. Empty inherits the global allowed_ports (else all ports). Applied in restricted and allowlist mode; ignored in open mode."
    }
  }
}

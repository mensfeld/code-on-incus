{
  "type": "object",
  "additionalProperties": false,
  "description": "Container image and build configuration",
  "properties": {
    "image": {
      "type": "string",
      "description": "Container image name to use"
    },
    "persistent": {
      "type": "boolean",
      "description": "Reuse the container across sessions instead of creating a fresh one"
    },
    "storage_pool": {
      "type": "string",
      "description": "Incus storage pool to use for this container"
    },
    "alias": {
      "type": "string",
      "description": "Human-readable alias for the workspace container"
    },
    "build": {
      "$ref": "#/$defs/BuildConfig"
    },
    "shutdown_timeout": {
      "type": "integer",
      "description": "Seconds to wait for graceful shutdown before force-killing (default: 60)"
    },
    "stale_base_check": {
      "type": "string",
      "enum": ["error", "warn", "off"],
      "description": "How to react when the project image's base is older than the current coi base: 'error' aborts, 'warn' prints a warning (default), 'off' is silent"
    }
  }
}
